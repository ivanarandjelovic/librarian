#Project build properties (folders, etc.)

src.dir=src
src.app.dir=${src.dir}/app
src.test.dir=${src.dir}/test
src.test.unit.dir=${src.test.dir}/unit
src.test.component.dir=${src.test.dir}/component
src.test.functional.dir=${src.test.dir}/functional
src.util.dir=${src.dir}/util

build.dir=build

bin.dir=bin
bin.app.dir=${bin.dir}/app
bin.test.dir=${bin.dir}/test
bin.test.unit.dir=${bin.test.dir}/unit
bin.test.component.dir=${bin.test.dir}/component
bin.test.functional.dir=${bin.test.dir}/functional
bin.instrumented.dir=${bin.dir}/instrumented-app
bin.util.dir=${bin.dir}/util

dist.dir=dist
dist.war.dir=${dist.dir}/war

lib.dir=lib
lib.app.dir=${lib.dir}/app
lib.cobertura.dir=${lib.dir}/cobertura
lib.jetty.dir=${lib.dir}/jetty
lib.jwebunit.dir=${lib.dir}/jwebunit
lib.test.dir=${lib.dir}/test
lib.support.dir=${lib.dir}/support
lib.db.driver.jar=mysql-connector-java-5.1.18-bin.jar

report.dir=report
report.junit.dir=${report.dir}/junit
report.junit-report.dir=${report.dir}/junit-report
report.coverage-report.dir=${report.dir}/coverage-report
report.cobertura.ser.file=${report.dir}/cobertura.ser

resources.dir=resources
resources.webinf.dir=${resources.dir}/WEB-INF
resources.classes.dir=${resources.dir}/classes

db.dir=db

config.dir=config

appStartupClassName=org.aivan.librarian.StartAppAsWar
	