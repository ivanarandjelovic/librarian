#Project build properties (folders, etc.)
src.dir=src
src.test.dir=test
src.test.unit.dir=${src.test.dir}/unit
src.test.component.dir=${src.test.dir}/component
build.dir=build
bin.dir=bin
dist.dir=dist
dist.war.dir=${dist.dir}/war
lib.dir=lib
logs.dir=log
logs.junit.dir=${logs.dir}/junit
logs.report.dir=${logs.dir}/test-report
resources.dir=resources
webinf.dir=${resources.dir}/WEB-INF
db.dir=db
config.dir=config
	